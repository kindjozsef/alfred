info:
  name: Models
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/models"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
